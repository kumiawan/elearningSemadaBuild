{
  "name": "dist",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss -i ./src/tailwind.css -o ./dist/output.css --watch",
    "autoreload": "browser-sync start --config bs-config.js",
    "phpserver": "php -S localhost:8000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "daisyui": "^4.11.1",
    "tailwindcss": "^3.4.3"
  },
  "dependencies": {
    "browser-sync": "^3.0.2"
  }
}
